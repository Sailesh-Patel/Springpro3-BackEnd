CREATE TABLE `property` (
	`id` INT PRIMARY KEY AUTO_INCREMENT,
	`address` VARCHAR,
	`location` VARCHAR,
	`price` INT,
	`type_of_property` VARCHAR,
	`bedrooms` INT,
	`bathrooms` INT,
	`garden` VARCHAR,
	`upload_images` VARCHAR,
	`property_status` VARCHAR
	);